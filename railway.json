{
  "$schema": "https://railway.app/railway.schema.json",
  "services": [
    {
      "name": "app",
      "builder": "DOCKERFILE",
      "env": {
        "SITE_NAME": "${{RAILWAY_PUBLIC_DOMAIN}}",
        "DB_HOST": "${{services.db.host}}",
        "DB_PORT": "${{services.db.port}}",
        "DB_DATABASE": "${{services.db.database}}",
        "DB_USER": "${{services.db.user}}",
        "DB_PASSWORD": "${{services.db.password}}",
        "REDIS_HOST": "${{services.redis.host}}",
        "REDIS_PORT": "${{services.redis.port}}"
      }
    },
    {
      "name": "db",
      "service": "mariadb"
    },
    {
      "name": "redis",
      "service": "redis"
    }
  ]
}
